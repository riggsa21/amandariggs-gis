<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Payments | GIS & Environmental Mapping</title>
  <meta name="description" content="Payment options for GIS & Environmental Mapping Services." />
  <style>
    :root{
      --ink:#0f172a; --muted:#334155; --brand:#2b65a6; --ring:#dbe3ef;
      --card:#ffffff; --bg1:#f7f9fb; --bg2:#dce6f2;
      --shadow-sm:0 4px 12px rgba(0,0,0,.06); --shadow:0 6px 15px rgba(0,0,0,.10);
      --ok:#16a34a;
    }
    html{scroll-behavior:smooth}
    *,*::before,*::after{box-sizing:border-box}
    body{
      margin:0;font-family:'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;color:var(--ink);
      background:linear-gradient(180deg, var(--bg1) 0%, var(--bg2) 100%);
      padding-top:56px;
    }
    a{color:#245c9c;text-decoration:none}
    a:hover{text-decoration:underline}

    /* Top bar */
    .topbar{position:fixed;inset:0 0 auto 0;height:56px;z-index:1000;display:flex;align-items:center;gap:12px;
      background:rgba(255,255,255,.9);backdrop-filter:blur(8px);border-bottom:1px solid #e5e7eb;padding:0 12px;}
    .brand{font-weight:800;color:var(--brand);letter-spacing:.2px;font-size:1.05rem;}
    .home-btn{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:10px;border:1.5px solid var(--brand);
      color:var(--brand);background:#fff;font-weight:700;text-decoration:none}

    main{max-width:1100px;margin:24px auto;padding:0 16px}
    .panel{background:#fff;border-radius:18px;box-shadow:var(--shadow-sm);padding:22px;margin-bottom:18px}
    h1{margin:12px 0 8px 0}
    h2{margin:0 0 10px 0;color:#24496f;border-bottom:2px solid var(--ring);padding-bottom:6px}

    /* Payment grid */
    .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:16px}
    .pay{border:1px solid #e5e7eb;border-radius:14px;padding:16px;background:#fff;box-shadow:var(--shadow-sm)}
    .pay h3{margin:0 0 6px 0;color:var(--brand)}
    .muted{color:#475569;font-size:.95rem;margin-top:4px}
    .btn{display:inline-flex;align-items:center;gap:8px;padding:10px 14px;border-radius:12px;font-weight:700;box-shadow:var(--shadow);border:none;cursor:pointer}
    .btn.primary{background:var(--brand);color:#fff}
    .btn.line{background:#fff;border:1.5px solid var(--brand);color:var(--brand)}
    .code{display:flex;align-items:center;gap:8px;margin-top:8px}
    .code input{flex:1;padding:10px 12px;border:1px solid #d6dee9;border-radius:10px;font-family:ui-monospace,SFMono-Regular,Menlo,monospace}
    .ok{color:var(--ok);font-weight:700;display:none;margin-left:8px}
    .qr{width:140px;height:140px;object-fit:contain;border-radius:10px;border:1px solid #e5e7eb;background:#f8fafc;margin-top:10px}
    .terms{color:#475569;font-size:.95rem}
  </style>
</head>
<body>

  <div class="topbar">
    <a class="home-btn" href="index.html">← Home</a>
    <div class="brand">Payments</div>
  </div>

  <main>
    <section class="panel">
      <h1>Payment Options</h1>
      <p class="terms">
        Typical terms: <strong>50% deposit</strong> to begin; balance at delivery. Invoices list scope and any third-party costs (datasets or hosting).
      </p>
    </section>

    <section class="panel">
      <h2>Choose a Method</h2>
      <div class="grid">

        <!-- PayPal -->
        <article class="pay" aria-label="PayPal">
          <h3>PayPal</h3>
          <p class="muted">Fast for cards or PayPal balance. Best for invoices.</p>
          <!-- Replace YOURPAYPALME with your link -->
          <p><a class="btn primary" href="https://paypal.me/YOURPAYPALME" target="_blank" rel="noopener">Open PayPal</a></p>

          <!-- Optional: show your PayPal email with copy -->
          <div class="code">
            <input id="ppEmail" value="amanda.l.riggs@outlook.com" aria-label="PayPal email" readonly>
            <button class="btn line" onclick="copy('ppEmail','ppOk')">Copy</button>
            <span id="ppOk" class="ok">Copied</span>
          </div>
          <!-- Optional QR (drop a PNG named paypal-qr.png in repo root and uncomment) -->
          <!-- <img class="qr" src="paypal-qr.png" alt="PayPal QR"> -->
        </article>

        <!-- Cash App -->
        <article class="pay" aria-label="Cash App">
          <h3>Cash App</h3>
          <p class="muted">Simple peer-to-peer payments using your $Cashtag.</p>
          <!-- Replace YOURCASH with your real $cashtag (no $ in the link path) -->
          <p><a class="btn primary" href="https://cash.app/$YOURCASH" target="_blank" rel="noopener">Pay via Cash App</a></p>

          <div class="code">
            <input id="cashTag" value="$YOURCASH" aria-label="Cash App cashtag" readonly>
            <button class="btn line" onclick="copy('cashTag','cashOk')">Copy</button>
            <span id="cashOk" class="ok">Copied</span>
          </div>
          <!-- Optional QR -->
          <!-- <img class="qr" src="cashapp-qr.png" alt="Cash App QR"> -->
        </article>

        <!-- Bitcoin via Coinbase -->
        <article class="pay" aria-label="Bitcoin">
          <h3>Bitcoin (via Coinbase)</h3>
          <p class="muted">Use Coinbase Commerce for a clean BTC checkout, or request a direct wallet address.</p>
          <!-- Replace CHECKOUT_ID with your Coinbase Commerce checkout id -->
          <p><a class="btn primary" href="https://commerce.coinbase.com/checkout/CHECKOUT_ID" target="_blank" rel="noopener">Pay with Bitcoin</a></p>

          <!-- Optional direct BTC address (only if you prefer) -->
          <div class="code">
            <input id="btcAddr" value="(on request)" aria-label="Bitcoin address" readonly>
            <button class="btn line" onclick="copy('btcAddr','btcOk')">Copy</button>
            <span id="btcOk" class="ok">Copied</span>
          </div>
          <!-- Optional QR -->
          <!-- <img class="qr" src="bitcoin-qr.png" alt="Bitcoin QR"> -->
        </article>

      </div>
    </section>

    <section class="panel">
      <h2>Notes</h2>
      <ul class="terms">
        <li>Invoices can be issued through PayPal (card accepted).</li>
        <li>Large or multi-phase projects can be split into milestones.</li>
        <li>Please include your <em>invoice number</em> or project title in the payment note.</li>
      </ul>
    </section>
  </main>

  <script>
    function copy(inputId, okId){
      const el = document.getElementById(inputId);
      el.select(); el.setSelectionRange(0, 99999);
      navigator.clipboard.writeText(el.value).then(()=>{
        const ok = document.getElementById(okId);
        ok.style.display='inline';
        setTimeout(()=> ok.style.display='none', 1500);
      });
    }
  </script>

</body>
</html>
